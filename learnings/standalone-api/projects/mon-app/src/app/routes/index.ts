import { Routes } from "@angular/router";
import { WookieListComponent } from "../features/wookie/wookie-list/wookie-list.component";

export const mainRoutes: Routes = [
  { path: '', component: WookieListComponent }
];

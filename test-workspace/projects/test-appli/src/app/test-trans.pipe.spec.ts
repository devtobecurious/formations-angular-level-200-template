import { TestTransPipe } from './test-trans.pipe';

describe('TestTransPipe', () => {
  it('create an instance', () => {
    const pipe = new TestTransPipe();
    expect(pipe).toBeTruthy();
  });
});
